server:
  port: 8082
  error:
    include-message: always
spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 215MB
  application:
    name: noteit
  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password: password
    # alternative datasource: external mysql database example
      url: jdbc:mysql://localhost:3306/NoteItDB?createDatabaseIfNotExist=true
      username: root
      password: password

  jpa:
    hibernate:
      ddl-auto: update

noteit:
  book:
    path: D:\\Projects\\Books\\